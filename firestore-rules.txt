// Firestore Security Rules for Backgammon Tournament App
// Copy these rules to your Firebase Console -> Firestore Database -> Rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Users collection - users can read/write their own profile
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      allow read: if request.auth != null; // Allow reading other users for tournament display
    }
    
    // Tournaments collection
    match /tournaments/{tournamentId} {
      // Anyone authenticated can read tournaments
      allow read: if request.auth != null;
      
      // Only tournament creator can create/update tournaments
      allow create: if request.auth != null && request.auth.uid == resource.data.createdBy;
      allow update: if request.auth != null && 
        (request.auth.uid == resource.data.createdBy || 
         request.auth.uid in resource.data.participants);
      
      // Only tournament creator can delete
      allow delete: if request.auth != null && request.auth.uid == resource.data.createdBy;
    }
    
    // Matches collection
    match /matches/{matchId} {
      // Anyone authenticated can read matches
      allow read: if request.auth != null;
      
      // Tournament creator or match participants can create/update matches
      allow create, update: if request.auth != null && 
        (request.auth.uid == resource.data.player1 || 
         request.auth.uid == resource.data.player2 ||
         request.auth.uid == resource.data.submittedBy);
      
      // Only tournament creator can delete matches
      allow delete: if request.auth != null;
    }
    
    // For testing purposes - REMOVE IN PRODUCTION
    // Uncomment the line below for initial testing, then remove it
    // match /{document=**} {
    //   allow read, write: if request.auth != null;
    // }
  }
}

// Instructions:
// 1. Go to Firebase Console
// 2. Select your project (shesh-besh-f9c84)
// 3. Go to Firestore Database
// 4. Click on "Rules" tab
// 5. Replace the existing rules with the rules above
// 6. Click "Publish"

// For initial testing, you can use the simple rule:
// match /{document=**} {
//   allow read, write: if request.auth != null;
// }

// Then switch to the more secure rules above once everything is working.